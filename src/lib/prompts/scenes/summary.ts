/**
 * Summary Requirement Prompt
 */

export const PROMPT_SUMMARY_REQUIREMENT = `
SUMMARY — 3-6 sentences. Prose writer's only brief.

Include: CHARACTER NAMES, LOCATION NAMES (not IDs), specifics (objects, dialogue), physical consequences.
Context: time span, technique, tone shifts.

BANNED: verbs (realizes, confirms, understands, suspects, decides) | endings (emotions, intentions) | modifiers (face etched, expression unreadable). Write ACTION, not thought.
`;
