// Thin re-export — the canonical CopyButton now lives in components/shared
// so the canvas top bar and the research panels both import from one place.
export { CopyButton } from "@/components/shared/CopyButton";
